# Lecture 2 — Algorithm Design

20 frames, no sections, 1 figures. Code listings: none.
Scheduled for **2 sessions**, assessed by **HW1**.

## Notes

**No `\section` commands and no code**, so it gets no outline dividers — it is
a short deck of flowcharts and pseudocode built with `algorithm2e`. That is
deliberate, not an omission: the point of the lecture is that an algorithm
exists before a language does. The smallest of the main decks at 20 frames.

## Reminders

- The deck sets only `\subtitle`; author, title and institute come from
  `../c-lecture.cls`. Do not set them here.
- Beamer does not fail on an overfull frame. Check the PDF after adding
  content, or use `[shrink]`.
