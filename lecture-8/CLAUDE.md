# Lecture 8 — Functions

87 frames, 8 sections, 3 figures. Code listings: `c`, `output`.
Scheduled for **3 sessions**, assessed by **HW3**.

## Sections

1. Introduction
2. Passing input parameters
3. Producing output
4. Scope of variables
5. Storage Class of variables
6. Function usage example
7. Recursion
8. Bugs & Avoiding Them

Each one produces an outline divider automatically, from the
`\AtBeginSection` hook in `../c-lecture.cls`.

## Notes

**The largest deck at 87 frames**, across three sessions. _Storage Class_ and
_Recursion_ are the two sections that need the most time. Pass-by-value is
established here and is what makes the pointers lecture land, so do not
compress the parameter-passing section.

## Reminders

- The deck sets only `\subtitle`; author, title and institute come from
  `../c-lecture.cls`. Do not set them here.
- Any frame containing `minted` must be `\begin{frame}[fragile]`.
- Beamer does not fail on an overfull frame. Check the PDF after adding
  content, or use `[shrink]`.
