# Lecture 13 — Miscellaneous

8 frames, no sections, no figures. Code listings: `c`.

## Notes

Eight frames, no sections, no figures. A deliberate offcuts deck for whatever
a given term has time for. It is not part of the schedule and should not grow
into a lecture without being given sections.

## Reminders

- The deck sets only `\subtitle`; author, title and institute come from
  `../c-lecture.cls`. Do not set them here.
- Any frame containing `minted` must be `\begin{frame}[fragile]`.
- Beamer does not fail on an overfull frame. Check the PDF after adding
  content, or use `[shrink]`.
